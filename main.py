#This is the main calculator

import Add
import subtract
import mul
import div

a=100
b=50

addition = Add.add(a,b)
substraction = substract.sub(a,b)
multiplication = mul.mul(a,b)
division = div.div(a,b)

print (f"Addition = {addition} Substraction = {substraction} Multiply = {multiplication} Divide = {division}  "
